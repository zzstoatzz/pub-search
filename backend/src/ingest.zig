pub const tap = @import("ingest/tap.zig");
pub const embedder = @import("ingest/embedder.zig");
pub const reconciler = @import("ingest/reconciler.zig");
// indexer and extractor are internal to the pipeline

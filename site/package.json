{
  "name": "leaflet-search-site",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev --port 8788",
    "deploy": "wrangler pages deploy"
  },
  "dependencies": {
    "workers-og": "^0.0.20"
  },
  "devDependencies": {
    "workbox-cli": "^7.4.1",
    "wrangler": "^4.0.0"
  }
}
